function functionName(parameters) {
    // function body
    return value; // optional
}  
const functionName = (parameters) =>{
    // function body
}

const calculateDiscount = function(price, discount){
    return price * (1 - discount);
}
const calculateShopDiscount =(price, discount) => price * (1 - discount);

const CustomerDiscount = (price, discount) => price * (1 - discount);

const studentAge = (currentYear, dateOfBirth) => (currentYear - dateOfBirth);
